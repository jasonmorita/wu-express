{{> top}}

	<h1>Choose...</h1>
	<ul>
		{{#each locations as |location|}}
			<li>
				<a href="/api/v1/conditions/{{location.state}}/{{location.city}}">{{location.city}}, {{location.state}}</a>
			</li>
		{{/each}}
	</ul>

{{> bottom}}
